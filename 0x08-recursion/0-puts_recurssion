#include "mainn.h"

/**
 * _puts_recursion - Prints a string followd 
 * @s: string
 *
 * Return success 1.
 *  set error
 */
void _puts_recursion(char *s)
{
if (*s == 0)
{
_putchar('\n');
return;
}
_putchar(*s);
_puts_recursion(s + 1);
}